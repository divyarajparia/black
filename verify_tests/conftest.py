# Minimal conftest — no custom markers needed for these tests.
# pytest --strict-markers is set in pyproject.toml addopts; our tests use
# no custom markers so this file just ensures the verify_tests directory
# is recognized as a package root.
